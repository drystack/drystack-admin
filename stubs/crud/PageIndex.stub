<?php

namespace {{namespace}};

use Illuminate\Support\Facades\Validator;
use Livewire\Component;

class {{name}}PageIndex extends Component
{
    public {{model}} $model;

    public string $title = "drystack::drystack.title.{{view}}.index"; //Key of translation string

    public function render()
    {
        return view('{{prefix}}.{{view}}-page-index')
            ->layout(config('drystack.layout'));
    }
}
